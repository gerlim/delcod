# DelCod release transition

This APK is signed with the fixed DelCod release key.
It is not referenced by updates/version.json because debug-signed installs cannot update to a release-signed APK in place.
Users moving from the debug-signed channel must uninstall the old app once, then install this APK.
