# DelCod release transition

Use DelCod-release-1.1.3-9.apk for the release-signed manual install.

The automatic updates/ channel remains debug-signed for old installs. Debug-signed installs cannot update to release-signed APKs in place. Users moving from the debug-signed channel must uninstall the old app once, then install this APK.
